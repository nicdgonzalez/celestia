# Todos

- [ ] Use `Celestia.lock` to manage plugins
- [ ] Subcommand for updating plugins
- [ ] Default to stable plugin version and include `--allow-experimental` flag
- [ ] Open JARs to read their config.yml and get their versions to auto-add to
  the manifest
